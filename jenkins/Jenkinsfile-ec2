pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {

        stage('Deploy Backend') {

            steps {

                sh '''
                ssh -o StrictHostKeyChecking=no ubuntu@13.228.39.122 "
                cd /opt/Backend-Deployment

                git pull origin main

                sudo systemctl restart backend

                sudo systemctl status backend --no-pager
                "
                '''
            }
        }
    }
}